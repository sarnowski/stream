# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=goldmont-plus -mtune=goldmont-plus -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

# parallelize to 2 CPUs if possible
MAKEOPTS="-j2"

GRUB_PLATFORMS="efi-64"
CPU_FLAGS_X86="aes mmx mmxext pclmul popcnt sha sse sse2 sse3 sse4_1 sse4_2 ssse3"
VIDEO_CARDS="intel i915"
INPUT_DEVICES="libinput"

USE="cups bluetooth png vorbis gif jpeg webp mp3 flac x264 opengl zsh-completion vim-syntax hwaccel filecaps dbus alsa pulseaudio ffmpeg truetype X xkb -joystick -ipv6"
